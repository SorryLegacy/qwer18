server {
        listen 80;
        server_name domain_name;

        location / {
                include proxy_params;
                proxy_pass http://unix:/name/project/project.sock>
                }
}
